// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://simonesaviolo.github.io',
	base: '/la-rete-www',
	trailingSlash: 'never',
});
